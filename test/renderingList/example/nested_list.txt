function PlaylistManager() {
  const playlists = [
    {
      id: 1,
      name: 'Chill Vibes',
      songs: [
        { id: 1, name: 'Peaceful Morning' },
        { id: 2, name: 'Evening Blues' }
      ]
    },
    {
      id: 2,
      name: 'Party Time',
      songs: [
        { id: 3, name: 'Dance All Night' },
        { id: 4, name: 'Electronic Beats' },
        { id: 5, name: 'Hip Hop Mix' }
      ]
    },
    {
      id: 3,
      name: 'Study Music',
      songs: [
        { id: 6, name: 'Focus Mode' }
      ]
    }
  ]

  return (
    <div style={{ padding: '20px', maxWidth: '600px', margin: '0 auto' }}>
      <h2>🎶 My Playlists</h2>

      {playlists.map(playlist => (
        <div
          key={playlist.id}
          style={{
            border: '2px solid #667eea',
            borderRadius: '8px',
            padding: '15px',
            marginBottom: '15px',
            background: '#f9f9f9'
          }}
        >
          <h3 style={{ marginTop: 0 }}>📋 {playlist.name}</h3>
          <p style={{ color: '#999', marginBottom: '10px' }}>({playlist.songs.length} songs)</p>

          <ol style={{ margin: 0 }}>
            {playlist.songs.map(song => (
              <li key={song.id} style={{ marginBottom: '8px' }}>
                <strong>{song.name}</strong>
              </li>
            ))}
          </ol>
        </div>
      ))}
    </div>
  )
}

export default PlaylistManager